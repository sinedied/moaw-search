{
  "name": "moaw-search",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "start": "concurrently npm:start:* --kill-others",
    "start:api": "docker compose up",
    "start:ui": "npm run start --workspace=search-ui"
  },
  "workspaces": [
    "packages/*"
  ],
  "license": "Apache-2.0",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
